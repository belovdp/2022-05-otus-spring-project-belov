spring:
  application:
    name: gateway-service
server:
  port: 8086
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
zuul:
  sensitiveHeaders:
  strip-prefix: false
gateway:
  cors: http://localhost:9000